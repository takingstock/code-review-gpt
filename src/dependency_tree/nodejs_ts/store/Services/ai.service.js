// const Models = require('../Models');

// // Get AIModelDATA from DB
// const findAll = (
//   criteria, projection, options, cb,
// ) => Models.AIModel.find(criteria, projection, options, cb);

// const findOne = (
//   criteria, projection, options, cb,
// ) => Models.AIModel.findOne(criteria, projection, options, cb);

// const count = (criteria, cb) => Models.AIModel.countDocuments(criteria, cb);

// // Insert AIModelUser in DB
// const create = (objToSave, cb) => new Models.AIModel(objToSave).save(cb);

// // const createMany = (arrayToSave, callback) => {
// //   Models.AIModel.insertMany(arrayToSave, callback);
// // };

// // Update AIModelUser in DB
// const update = (criteria, dataToSet, options = {}, cb) => Models.AIModel
//   .findOneAndUpdate(criteria, dataToSet, options, cb);

// // const updateOne = (criteria, dataToSet, options = {}, cb) => Models.AIModel
// //   .updateOne(criteria, dataToSet, options, cb);

// const updateAll = (criteria, dataToSet, options, cb) => Models.AIModel
//   .updateMany(criteria, dataToSet, options, cb);

// const deleteOne = (criteria, cb) => Models.AIModel
//   .deleteOne(criteria, cb);


// module.exports = {
//   findAll,
//   findOne,
//   count,
//   update,
//   //updateOne,
//   updateAll,
//   create,
//   //createMany,
//   deleteOne,
// };
